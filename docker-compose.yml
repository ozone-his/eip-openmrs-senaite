version: '3'
services:
  ozone-eip-client:
    container_name: eip-client
    image: eip-client:latest
    ports:
      - "8085:8080"
    env_file:
      - .env
    volumes:
      - ./target/eip-openmrs-senaite-1.3.0-SNAPSHOT.jar:/eip-client/routes/eip-openmrs-senaite-1.3.0-SNAPSHOT.jar
